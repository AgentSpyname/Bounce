
 <div class="status">
 <div class="span1">

 <%= link_to image_tag(@status.user.gravatar_url), profile_path(@status.user.username) %><strong><%= @status.user.first_name + " " +  @status.user.last_name %> (@<%= @status.user.username %>) Says:</strong> 
</div>
<p>
 <br />
  <%= @status.content %>
</p>
<p>

<br />
 <% if !@status.picture_file_name.nil? %>

<%= image_tag @status.picture.url(:medium) %>
<% else %>

<% end %>

</p>

<%= link_to 'Back', root_url %>
<% if current_user %>
<% if current_user.username == @status.user.username %>|
<%= link_to 'Edit', edit_status_path(@status) %> 

<% end %>



<% end %>


<br />
</div>
<div class="page-header">
<h1> Comments </h1>
</div>

<% if current_user %>
<div class="status">
<%= link_to  "New Comment",comment_status_path,class: "btn btn-success"  %><br /> <br />
</div>
<% end %>
<% @comments.each do |comment| %>
<div class="status">
  <div class="span1">

 <%= link_to image_tag(comment.user.gravatar_url) %>    <strong><%= comment.user.first_name + " " + comment.user.last_name %> Says...</strong>

  
 </div>
   <div class= "span7">

</div>
  <p><%= comment.comment %></p>
  <div class="meta">
    <%= link_to time_ago_in_words(comment.created_at) + " ago" %>

     | <%= link_to "Edit", edit_comment_path(comment) %> | 
     <%= link_to "Delete", comment, method: :delete, data: { confirm: "Are you sure your want to delete this comment?"} %>
 

  
   </span>
  </div>
</div>

 
      <% end %>



    


<br />

